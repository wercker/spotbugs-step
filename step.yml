name: spotbugs 
version: 3.1.2
keywords:
  - spotbugs
summary: This step runs SpotBugs on the current Java project end output the result to the output according to the format specified.
properties:
  - name: output 
    type: string
    required: true
  - name: format
    type: string
    required: true
